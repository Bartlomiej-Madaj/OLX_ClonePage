import styled from 'styled-components';

export const StyledAdvertisementPage = styled.section`
    width: 100%;
    height: 100%;
    box-sizing: border-box;
    background-color: ${props => props.theme.mainColor};
    overflow: hidden;
`;