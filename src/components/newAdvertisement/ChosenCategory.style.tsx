import styled from 'styled-components';

export const StyledChosenCategory = styled.div`
    width: 90%;
    max-width: 1200px;
    position:absolute;
    z-index: 2;
    top: 10%;
    left: 50%;
    transform: translateX(-50%);
    padding: 1rem;

    section {
        box-sizing: border-box;
        padding: 1rem;
    }

     h1 {
        margin-top: 1rem;
        padding-top: 1rem;
        background-color: white;
     }

`;